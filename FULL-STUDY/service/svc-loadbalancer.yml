apiVersion: v1
kind: Service
metadata:
  name: loadbalancer-ip
  namespace: for-svc
  labels:
    svc: service-label
spec:
  type: LoadBalancer
  selector:
    app: nginx-svc-test ## should match with the pods label
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80 